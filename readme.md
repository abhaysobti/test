## This is how I worked on Git
```
git remote add origin https://github.com/abhaysobti/test.git

git add .

git commit -m "Initial Commit by Me"

git push -u origin main   -- Required authentication
```

